{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "FastAPI: Debug server.py",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/app/backend/server.py",
            "console": "integratedTerminal",
            "args": [
                "--reload",
                "--host", "127.0.0.1",
                "--port", "8000"
            ],
            "jinja": true
        }
    ]
}
